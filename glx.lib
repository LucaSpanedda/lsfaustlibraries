// THIS library
glx = library("glx.lib");
// Luca Spanedda's Standard Faust Libraries
import("stdls.lib");

/*
Glitch Library
*/


//------------------------------------------------------------------ SIGNALS ---

// pulse train based on primeNoise
rndmpulse(index, treshold) = primeNoise(index) > (1-treshold) : \(y).( y > y' );

noiseClicks(index,f,ms) = (ramp(pulse(f),ms) : sine) * primeNoise(index);
//process = noiseClicks(8,16,.1), noiseClicks(10,16,.1);